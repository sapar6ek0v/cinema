import styled from 'styled-components';
import { colors } from '../../constants/colors';

export const Wrapper = styled.section`
  min-height: 500px;
  background: ${colors.bg3} url('./images/sr-single.jpg') center/cover;
  background-blend-mode: darken;
  padding: 50px 0px;
`;
